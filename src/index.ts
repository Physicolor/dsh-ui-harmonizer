/**
 * Harness UI Enhancer — host (node) half.
 *
 * Deliberately empty. Everything this plugin does is browser-side: CSS
 * variable overrides and settings-page slot registration. There is no host
 * service, tool, or event contribution, so the node half is a no-op apply
 * that keeps the loader row mountable.
 */

/** Host plugin body — intentionally empty; see file header. */
export function apply(): void {}
